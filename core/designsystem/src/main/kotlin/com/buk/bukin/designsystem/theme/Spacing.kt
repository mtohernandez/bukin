package com.buk.bukin.designsystem.theme

import androidx.compose.ui.unit.dp

/**
 * The spacing scale. Features use these rather than literal dp so the rhythm stays in one
 * place — same reason colours live in [Color].
 */
object BukSpacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
    val xl = 32.dp
    val xxl = 48.dp

    /** Horizontal page gutter, matching the mockups. */
    val gutter = 20.dp
}
